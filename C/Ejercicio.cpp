#include <iostream> 
using namespace std; 

int main () 
{
int n, num,celsius;
float resultado;
//int mayor = menor = sum = media = 0;

cout<<"1.- Convertir Grados Celsius a Farenheit"<<endl;
cout<<"2.- Convertir Grados de Farenheit a Celsius "<<endl;
cout<<"3.- Convertir Grados Celsius a Kelvin"<<endl;
cin>> n;

cout<<"Ingrese un numero: "; cin>>num;
if(i == 1)
{
    cout<<"Digite los grados Celsius a vconvertir"<<endl;
    cin>>celsius;
    resultado=(celsius * 9/5) + 32
    
    mayor = menor = num;}
/* if (num > mayor)
{ mayor = num; }
if (num < menor)
{ menor = num; }
sum += num;


media = sum / n;
cout<<"El numero mayor es: "<<mayor<<endl;
cout<<"El numero menor es: "<<menor<<endl;
cout<<"La media total es: "<<media<<endl;
 */
return 0;
}